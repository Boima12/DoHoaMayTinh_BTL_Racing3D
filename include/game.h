#ifndef GAME_H
#define GAME_H

// Minimal game interface used by the app skeleton.
// Real implementation will replace these later.
namespace Game {
	void init();
	void update(float dt);
	void render();
}

#endif // GAME_H
